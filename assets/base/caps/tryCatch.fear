package base.caps

/// This try-catch will catch non-deterministic errors.
/// This also does not uphold strong exception safety, so you can observe changes to the program state from
/// exception-causing code.
CapTries: F[mut System, mut CapTry]{s -> mut CapTry: {
  mut #[R](try: mut TryBody[R]): mut Res[R, Info] -> Magic!,
  }}

TryBody[R]: {mut #: R}
