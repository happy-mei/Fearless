package base;
public interface IsoPod_0{
IsoPod_0 $self = new IsoPod_0Impl();
base.IsoPod_1 $hash$imm(Object val_m$);
static base.IsoPod_1 $hash$imm$fun(Object val_m$, base.IsoPod_0 $this) {
  return ((base.IsoPod_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.IsoPod_1) null;
}})
);
}
}