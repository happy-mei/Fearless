package base.flows;
public interface EmptyFlow_1 extends base.flows.Flow_1{
EmptyFlow_1 $self = new EmptyFlow_1Impl();
base.Void_0 forEffect$mut(base.MF_2 f_m$);

base.flows.Flow_1 peek$mut(base.F_2 f_m$);

base.flows.Flow_1 peek$mut(base.ToIso_1 ctx_m$, base.F_3 f_m$);

base.flows.Flow_1 actorMut$mut(Object state_m$, base.flows.ActorImplMut_3 f_m$);

base.flows.Flow_1 self$mut();

base.flows.Flow_1 self$read();

base.flows.Flow_1 self$imm();

Object let$mut(base.F_2 x_m$, base.Continuation_3 cont_m$);

base.flows.Flow_1 assumeFinite$mut();

base.flows.Flow_1 actor$mut(Object state_m$, base.flows.ActorImpl_3 f_m$);

base.Opt_1 first$mut();

base.List_1 list$mut();

base.Bool_0 all$mut(base.F_2 p_m$);

base.flows.Flow_1 filter$mut(base.F_2 p_m$);

base.Bool_0 none$mut(base.F_2 predicate_m$);

base.flows.Flow_1 flatMap$mut(base.F_2 f_m$);

base.Opt_1 opt$mut();

Object get$mut();

Long count$mut();

base.Opt_1 findMap$mut(base.F_2 fear2192$_m$);

Object join$mut(base.flows.Joinable_1 j_m$);

base.Opt_1 last$mut();

base.flows.FlowOp_1 unwrapOp$mut(base.flows._UnwrapFlowToken_0 fear2193$_m$);

base.flows.Flow_1 mapFilter$mut(base.F_2 f_m$);

Object fold$mut(base.MF_1 acc_m$, base.F_3 fear2194$_m$);

base.Action_1 only$mut();

base.Opt_1 first$mut(base.F_2 predicate_m$);

base.flows.Flow_1 map$mut(base.ToIso_1 fear2195$_m$, base.F_3 fear2196$_m$);

base.flows.Flow_1 map$mut(base.F_2 f_m$);

base.flows.Flow_1 limit$mut(long fear2197$_m$);

base.Bool_0 any$mut(base.F_2 p_m$);

base.Opt_1 find$mut(base.F_2 fear2198$_m$);

base.Opt_1 max$mut(base.F_3 compare_m$);

base.Opt_1 size$read();

base.flows.Flow_1 scan$mut(Object acc_m$, base.F_3 f_m$);

Object $hash$imm(base.Extension_2 ext_m$);

Object $hash$read(base.Extension_2 ext_m$);

Object $hash$mut(base.Extension_2 ext_m$);

base.Void_0 for$mut(base.F_2 f_m$);
static base.flows.Flow_1 filter$mut$fun(base.F_2 p_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.flows.Flow_1 map$mut$fun(base.F_2 f_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.flows.Flow_1 map$mut$fun(base.ToIso_1 fear2185$_m$, base.F_3 fear2186$_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.flows.Flow_1 flatMap$mut$fun(base.F_2 f_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.flows.Flow_1 actor$mut$fun(Object state_m$, base.flows.ActorImpl_3 f_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.flows.Flow_1 actorMut$mut$fun(Object state_m$, base.flows.ActorImplMut_3 f_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.flows.Flow_1 limit$mut$fun(long fear2187$_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.Opt_1 first$mut$fun(base.flows.EmptyFlow_1 $this) {
  return ((base.Opt_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Opt_1) null;
}})
);
}

static base.Opt_1 findMap$mut$fun(base.F_2 fear2188$_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.Opt_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Opt_1) null;
}})
);
}

static base.Opt_1 find$mut$fun(base.F_2 fear2189$_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.Opt_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Opt_1) null;
}})
);
}

static base.Bool_0 any$mut$fun(base.F_2 p_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.Bool_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Bool_0) null;
}})
);
}

static base.Bool_0 all$mut$fun(base.F_2 p_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.Bool_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Bool_0) null;
}})
);
}

static Object fold$mut$fun(base.MF_1 acc_m$, base.F_3 fear2190$_m$, base.flows.EmptyFlow_1 $this) {
  return (switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (Object) null;
}})
;
}

static base.flows.Flow_1 assumeFinite$mut$fun(base.flows.EmptyFlow_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.Opt_1 size$read$fun(base.flows.EmptyFlow_1 $this) {
  return ((base.Opt_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Opt_1) null;
}})
);
}

static Long count$mut$fun(base.flows.EmptyFlow_1 $this) {
  return ((long)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (long) 0;
}})
);
}

static base.flows.FlowOp_1 unwrapOp$mut$fun(base.flows._UnwrapFlowToken_0 fear2191$_m$, base.flows.EmptyFlow_1 $this) {
  return ((base.flows.FlowOp_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.FlowOp_1) null;
}})
);
}
}