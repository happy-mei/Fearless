package base.flows;
public interface _TerminalOps_1 extends base.Sealed_0{
base.Void_0 forEffect$mut(base.MF_2 f_m$);

Long count$mut();

base.Opt_1 findMap$mut(base.F_2 f_m$);

base.Opt_1 last$mut();

Object fold$mut(base.MF_1 acc_m$, base.F_3 f_m$);

base.Opt_1 first$mut(base.F_2 predicate_m$);

base.Bool_0 any$mut(base.F_2 predicate_m$);

base.Opt_1 first$mut();

base.Opt_1 find$mut(base.F_2 predicate_m$);

base.Opt_1 max$mut(base.F_3 compare_m$);

base.List_1 list$mut();

base.Opt_1 size$read();

base.Bool_0 all$mut(base.F_2 predicate_m$);

base.Bool_0 none$mut(base.F_2 predicate_m$);

base.Void_0 for$mut(base.F_2 f_m$);
static base.Opt_1 last$mut$fun(base.flows._TerminalOps_1 $this) {
  return ((base.Opt_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Opt_1) null;
}})
);
}

static base.Opt_1 find$mut$fun(base.F_2 predicate_m$, base.flows._TerminalOps_1 $this) {
  return ((base.Opt_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Opt_1) null;
}})
);
}

static base.Opt_1 first$mut$fun(base.F_2 predicate_m$, base.flows._TerminalOps_1 $this) {
  return ((base.Opt_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Opt_1) null;
}})
);
}

static base.Bool_0 any$mut$fun(base.F_2 predicate_m$, base.flows._TerminalOps_1 $this) {
  return ((base.Bool_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Bool_0) null;
}})
);
}

static base.Bool_0 all$mut$fun(base.F_2 predicate_m$, base.flows._TerminalOps_1 $this) {
  return ((base.Bool_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Bool_0) null;
}})
);
}

static base.Bool_0 none$mut$fun(base.F_2 predicate_m$, base.flows._TerminalOps_1 $this) {
  return ((base.Bool_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Bool_0) null;
}})
);
}

static base.Void_0 for$mut$fun(base.F_2 f_m$, base.flows._TerminalOps_1 $this) {
  return ((base.Void_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Void_0) null;
}})
);
}

static base.Void_0 forEffect$mut$fun(base.MF_2 f_m$, base.flows._TerminalOps_1 $this) {
  return ((base.Void_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Void_0) null;
}})
);
}

static base.List_1 list$mut$fun(base.flows._TerminalOps_1 $this) {
  return ((base.List_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.List_1) null;
}})
);
}

static base.Opt_1 max$mut$fun(base.F_3 compare_m$, base.flows._TerminalOps_1 $this) {
  return ((base.Opt_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Opt_1) null;
}})
);
}
}