package base.json;
public interface _ParseHex_0{
_ParseHex_0 $self = new _ParseHex_0Impl();
Long encoding$imm(rt.Str ch_m$);

Long $hash$imm(base.List_1 chars_m$);
static Long $hash$imm$fun(base.List_1 chars_m$, base.json._ParseHex_0 $this) {
  return ((long)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (long) 0;
}})
);
}

static Long encoding$imm$fun(rt.Str ch_m$, base.json._ParseHex_0 $this) {
  return ((long)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (long) 0;
}})
);
}
}