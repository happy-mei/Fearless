package base.json;
public interface ParseJson_0{
ParseJson_0 $self = new ParseJson_0Impl();
base.Action_1 $hash$imm(rt.Str rawInput_m$);
static base.Action_1 $hash$imm$fun(rt.Str rawInput_m$, base.json.ParseJson_0 $this) {
  return ((base.Action_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Action_1) null;
}})
);
}
}