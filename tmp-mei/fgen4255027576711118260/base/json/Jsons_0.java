package base.json;
public interface Jsons_0{
Jsons_0 $self = new Jsons_0Impl();
base.json.Json_0 bool$imm(base.Bool_0 b_m$);

base.json.Json_0 number$imm(double n_m$);

base.json.Json_0 array$imm(base.List_1 a_m$);

base.json.Json_0 object$imm(base.LinkedHashMap_2 o_m$);

base.json.Json_0 null$imm();

base.json.Json_0 string$imm(rt.Str s_m$);
static base.json.Json_0 string$imm$fun(rt.Str s_m$, base.json.Jsons_0 $this) {
  return ((base.json.Json_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.json.Json_0) null;
}})
);
}

static base.json.Json_0 number$imm$fun(double n_m$, base.json.Jsons_0 $this) {
  return ((base.json.Json_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.json.Json_0) null;
}})
);
}

static base.json.Json_0 bool$imm$fun(base.Bool_0 b_m$, base.json.Jsons_0 $this) {
  return ((base.json.Json_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.json.Json_0) null;
}})
);
}

static base.json.Json_0 object$imm$fun(base.LinkedHashMap_2 o_m$, base.json.Jsons_0 $this) {
  return ((base.json.Json_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.json.Json_0) null;
}})
);
}

static base.json.Json_0 array$imm$fun(base.List_1 a_m$, base.json.Jsons_0 $this) {
  return ((base.json.Json_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.json.Json_0) null;
}})
);
}

static base.json.Json_0 null$imm$fun(base.json.Jsons_0 $this) {
  return ((base.json.Json_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.json.Json_0) null;
}})
);
}
}