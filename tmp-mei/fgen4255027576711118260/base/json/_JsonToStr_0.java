package base.json;
public interface _JsonToStr_0{
_JsonToStr_0 $self = new _JsonToStr_0Impl();
rt.Str array$imm(base.List_1 a_m$);

rt.Str escape$imm(rt.Str s_m$);

rt.Str object$imm(base.LinkedHashMap_2 o_m$);

rt.Str $hash$imm(base.json.Json_0 json_m$);
static rt.Str $hash$imm$fun(base.json.Json_0 json_m$, base.json._JsonToStr_0 $this) {
  return ((rt.Str)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (rt.Str) null;
}})
);
}

static rt.Str array$imm$fun(base.List_1 a_m$, base.json._JsonToStr_0 $this) {
  return ((rt.Str)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (rt.Str) null;
}})
);
}

static rt.Str object$imm$fun(base.LinkedHashMap_2 o_m$, base.json._JsonToStr_0 $this) {
  return ((rt.Str)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (rt.Str) null;
}})
);
}

static rt.Str escape$imm$fun(rt.Str s_m$, base.json._JsonToStr_0 $this) {
  return ((rt.Str)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (rt.Str) null;
}})
);
}
}