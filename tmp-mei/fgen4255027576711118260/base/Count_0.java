package base;
public interface Count_0{
Count_0 $self = new Count_0Impl();
base.Count_1 nat$imm(long n_m$);

base.Count_1 int$imm(long n_m$);

base.Count_1 $hash$imm(long n_m$);
static base.Count_1 $hash$imm$fun(long n_m$, base.Count_0 $this) {
  return ((base.Count_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Count_1) null;
}})
);
}

static base.Count_1 int$imm$fun(long n_m$, base.Count_0 $this) {
  return ((base.Count_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Count_1) null;
}})
);
}

static base.Count_1 nat$imm$fun(long n_m$, base.Count_0 $this) {
  return ((base.Count_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Count_1) null;
}})
);
}
}