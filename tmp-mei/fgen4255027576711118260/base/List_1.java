package base;
public interface List_1 extends base.Collection_0,base.Sealed_0{
base.flows.FlowOp_1 _flowimm$imm(long start_m$, long end_m$);

Object get$imm(long i_m$);

Object get$read(long i_m$);

Object get$mut(long i_m$);

base.Void_0 addAll$mut(base.List_1 other_m$);

base.Opt_1 tryGet$imm(long i_m$);

base.Opt_1 tryGet$read(long i_m$);

base.Opt_1 tryGet$mut(long i_m$);

base.iter.Iter_1 iter$imm();

base.iter.Iter_1 iter$read();

base.iter.Iter_1 iter$mut();

base.flows.Flow_1 flow$imm();

base.flows.Flow_1 flow$read();

base.flows.Flow_1 flow$mut();

base.Bool_0 isEmpty$read();

base.Void_0 clear$mut();

base.ListView_1 subList$read(long from_m$, long to_m$);

base.List_1 as$read(base.MF_2 f_m$);

Long size$read();

base.Opt_1 takeFirst$mut();

base.List_1 $plus$mut(Object e_m$);

base.flows.FlowOp_1 _flowread$read(long start_m$, long end_m$);

base.Void_0 add$mut(Object e_m$);
static base.List_1 $plus$mut$fun(Object e_m$, base.List_1 $this) {
  return ((base.List_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.List_1) null;
}})
);
}

static base.Void_0 addAll$mut$fun(base.List_1 other_m$, base.List_1 $this) {
  return ((base.Void_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Void_0) null;
}})
);
}

static base.List_1 as$read$fun(base.MF_2 f_m$, base.List_1 $this) {
  return ((base.List_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.List_1) null;
}})
);
}

static base.flows.Flow_1 flow$mut$fun(base.List_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.flows.Flow_1 flow$read$fun(base.List_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.flows.FlowOp_1 _flowread$read$fun(long start_m$, long end_m$, base.List_1 $this) {
  return ((base.flows.FlowOp_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.FlowOp_1) null;
}})
);
}

static base.flows.Flow_1 flow$imm$fun(base.List_1 $this) {
  return ((base.flows.Flow_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.Flow_1) null;
}})
);
}

static base.flows.FlowOp_1 _flowimm$imm$fun(long start_m$, long end_m$, base.List_1 $this) {
  return ((base.flows.FlowOp_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.flows.FlowOp_1) null;
}})
);
}

static base.iter.Iter_1 iter$mut$fun(base.List_1 $this) {
  return ((base.iter.Iter_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.iter.Iter_1) null;
}})
);
}

static base.iter.Iter_1 iter$read$fun(base.List_1 $this) {
  return ((base.iter.Iter_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.iter.Iter_1) null;
}})
);
}

static base.iter.Iter_1 iter$imm$fun(base.List_1 $this) {
  return ((base.iter.Iter_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.iter.Iter_1) null;
}})
);
}

static base.ListView_1 subList$read$fun(long from_m$, long to_m$, base.List_1 $this) {
  return ((base.ListView_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.ListView_1) null;
}})
);
}
}