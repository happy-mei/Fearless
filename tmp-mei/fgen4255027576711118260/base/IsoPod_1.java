package base;
public interface IsoPod_1{
Object consume$mut(base.IsoConsumer_2 f_m$);

Object $exclamation$mut();

base.Action_1 look$read(base.IsoViewer$apostrophe_2 f_m$);

Object peek$read(base.IsoViewer_2 f_m$);

base.Action_1 mutate$mut(base.F_2 f_m$);

base.Bool_0 isDead$read();

base.Void_0 next$mut(Object val_m$);

base.Bool_0 isAlive$read();

base.Void_0 $colon$equals$mut(Object val_m$);
static base.Action_1 look$read$fun(base.IsoViewer$apostrophe_2 f_m$, base.IsoPod_1 $this) {
  return ((base.Action_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Action_1) null;
}})
);
}

static base.Bool_0 isDead$read$fun(base.IsoPod_1 $this) {
  return ((base.Bool_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Bool_0) null;
}})
);
}

static Object consume$mut$fun(base.IsoConsumer_2 f_m$, base.IsoPod_1 $this) {
  return (switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (Object) null;
}})
;
}

static base.Void_0 $colon$equals$mut$fun(Object val_m$, base.IsoPod_1 $this) {
  return ((base.Void_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Void_0) null;
}})
);
}

static base.Action_1 mutate$mut$fun(base.F_2 f_m$, base.IsoPod_1 $this) {
  return ((base.Action_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Action_1) null;
}})
);
}
}