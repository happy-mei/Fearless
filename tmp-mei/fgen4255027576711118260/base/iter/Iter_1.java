package base.iter;
public interface Iter_1{
Long count$mut();

base.Bool_0 allMatch$mut(base.iter.IterPredicate_1 p_m$);

Object fold$mut(Object acc_m$, base.iter.IterFolder_2 f_m$);

base.Opt_1 find$mut(base.iter.IterPredicate_1 p_m$);

base.List_1 list$mut();

base.LList_1 llist$mut();

base.iter.Iter_1 filter$mut(base.iter.IterPredicate_1 p_m$);

base.Opt_1 next$mut();

rt.Str str$mut(base.iter.IterMapper_2 toStr_m$, rt.Str joining_m$);

base.Bool_0 anyMatch$mut(base.iter.IterPredicate_1 p_m$);

base.Void_0 for$mut(base.iter.IterForEacher_1 f_m$);

base.iter.Iter_1 flatMap$mut(base.iter.IterMapper_2 f_m$);

base.iter.Iter_1 map$mut(base.iter.IterMapper_2 f_m$);
static base.Opt_1 find$mut$fun(base.iter.IterPredicate_1 p_m$, base.iter.Iter_1 $this) {
  return ((base.Opt_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Opt_1) null;
}})
);
}

static base.Bool_0 allMatch$mut$fun(base.iter.IterPredicate_1 p_m$, base.iter.Iter_1 $this) {
  return ((base.Bool_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Bool_0) null;
}})
);
}

static base.Bool_0 anyMatch$mut$fun(base.iter.IterPredicate_1 p_m$, base.iter.Iter_1 $this) {
  return ((base.Bool_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Bool_0) null;
}})
);
}

static base.iter.Iter_1 map$mut$fun(base.iter.IterMapper_2 f_m$, base.iter.Iter_1 $this) {
  return ((base.iter.Iter_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.iter.Iter_1) null;
}})
);
}

static base.iter.Iter_1 flatMap$mut$fun(base.iter.IterMapper_2 f_m$, base.iter.Iter_1 $this) {
  return ((base.iter.Iter_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.iter.Iter_1) null;
}})
);
}

static base.iter.Iter_1 filter$mut$fun(base.iter.IterPredicate_1 p_m$, base.iter.Iter_1 $this) {
  return ((base.iter.Iter_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.iter.Iter_1) null;
}})
);
}

static Object fold$mut$fun(Object acc_m$, base.iter.IterFolder_2 f_m$, base.iter.Iter_1 $this) {
  return (switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (Object) null;
}})
;
}

static base.Void_0 for$mut$fun(base.iter.IterForEacher_1 f_m$, base.iter.Iter_1 $this) {
  return ((base.Void_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Void_0) null;
}})
);
}

static Long count$mut$fun(base.iter.Iter_1 $this) {
  return ((long)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (long) 0;
}})
);
}

static base.List_1 list$mut$fun(base.iter.Iter_1 $this) {
  return ((base.List_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.List_1) null;
}})
);
}

static base.LList_1 llist$mut$fun(base.iter.Iter_1 $this) {
  return ((base.LList_1)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.LList_1) null;
}})
);
}

static rt.Str str$mut$fun(base.iter.IterMapper_2 toStr_m$, rt.Str joining_m$, base.iter.Iter_1 $this) {
  return ((rt.Str)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (rt.Str) null;
}})
);
}
}