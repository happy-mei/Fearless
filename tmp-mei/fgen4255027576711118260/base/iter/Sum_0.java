package base.iter;
public interface Sum_0{
Sum_0 $self = new Sum_0Impl();
Long nat$imm(base.iter.Iter_1 ns_m$);

Double float$imm(base.iter.Iter_1 ns_m$);

Long int$imm(base.iter.Iter_1 ns_m$);
static Long int$imm$fun(base.iter.Iter_1 ns_m$, base.iter.Sum_0 $this) {
  return ((long)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (long) 0;
}})
);
}

static Long nat$imm$fun(base.iter.Iter_1 ns_m$, base.iter.Sum_0 $this) {
  return ((long)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (long) 0;
}})
);
}

static Double float$imm$fun(base.iter.Iter_1 ns_m$, base.iter.Sum_0 $this) {
  return ((double)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (double) 0;
}})
);
}
}