package base;
public interface FOrdering_0{
FOrdering_0 $self = new FOrdering_0Impl();
base.Ordering_0 greater$imm();

base.Ordering_0 less$imm();

base.Ordering_0 equal$imm();
static base.Ordering_0 less$imm$fun(base.FOrdering_0 $this) {
  return ((base.Ordering_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Ordering_0) null;
}})
);
}

static base.Ordering_0 equal$imm$fun(base.FOrdering_0 $this) {
  return ((base.Ordering_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Ordering_0) null;
}})
);
}

static base.Ordering_0 greater$imm$fun(base.FOrdering_0 $this) {
  return ((base.Ordering_0)(switch (1) { default -> {
  System.err.println("Program aborted at:\n"+java.util.Arrays.stream(Thread.currentThread().getStackTrace()).map(StackTraceElement::toString).collect(java.util.stream.Collectors.joining("\n")));
  System.exit(1);
  yield (base.Ordering_0) null;
}})
);
}
}