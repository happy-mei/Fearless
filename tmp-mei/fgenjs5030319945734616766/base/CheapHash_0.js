import { base$$Hasher_0 } from "../base/Hasher_0.js";
import * as rt from "../rt/main.js";

export class base$$CheapHash_0 extends base$$Hasher_0 {
  static $self = new base$$CheapHash_0();

  hash$mut(x_m$) {
  return base$$Hasher_0.hash$mut$fun(x_m$, this);
}


byte$mut(x_m$) {
  return base$$CheapHash_0.byte$mut$fun(x_m$, this);
}


str$mut(x_m$) {
  return base$$CheapHash_0.str$mut$fun(x_m$, this);
}


nat$mut(x_m$) {
  return base$$CheapHash_0.nat$mut$fun(x_m$, this);
}


float$mut(x_m$) {
  return base$$CheapHash_0.float$mut$fun(x_m$, this);
}


int$mut(x_m$) {
  return base$$CheapHash_0.int$mut$fun(x_m$, this);
}


compute$mut() {
  return base$$CheapHash_0.compute$mut$fun(this);
}

  static compute$mut$fun($this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static nat$mut$fun(x_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static int$mut$fun(x_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static float$mut$fun(x_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static byte$mut$fun(x_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static str$mut$fun(x_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
