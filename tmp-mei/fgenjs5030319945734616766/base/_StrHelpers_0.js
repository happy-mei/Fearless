import * as rt from "../rt/main.js";
export class base$$_StrHelpers_0 {
  static $self = new base$$_StrHelpers_0();

  assertEq$imm(expected_m$, actual_m$) {
  return base$$_StrHelpers_0.assertEq$imm$fun(expected_m$, actual_m$, this);
}


assertEq$imm(expected_m$, actual_m$, message_m$) {
  return base$$_StrHelpers_0.assertEq$imm$fun(expected_m$, actual_m$, message_m$, this);
}

  static assertEq$imm$fun(expected_m$, actual_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static assertEq$imm$fun(expected_m$, actual_m$, message_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
