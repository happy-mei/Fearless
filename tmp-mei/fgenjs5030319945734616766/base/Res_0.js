import * as rt from "../rt/main.js";
export class base$$Res_0 {
  static $self = new base$$Res_0();

  err$imm(x_m$) {
  return base$$Res_0.err$imm$fun(x_m$, this);
}


ok$imm(x_m$) {
  return base$$Res_0.ok$imm$fun(x_m$, this);
}


$hash$imm(x_m$) {
  return base$$Res_0.$hash$imm$fun(x_m$, this);
}

  static $hash$imm$fun(x_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static ok$imm$fun(x_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static err$imm$fun(x_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
