import { base$$F_3 } from "../base/F_3.js";
import * as rt from "../rt/main.js";

export class base$$CompareFloats_0 extends base$$F_3 {
  static $self = new base$$CompareFloats_0();

  $hash$read(a_m$, b_m$) {
  return base$$CompareFloats_0.$hash$read$fun(a_m$, b_m$, this);
}

  static $hash$read$fun(a_m$, b_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
