import * as rt from "../rt/main.js";
export class base$$IsoPod_0 {
  static $self = new base$$IsoPod_0();

  $hash$imm(val_m$) {
  return base$$IsoPod_0.$hash$imm$fun(val_m$, this);
}

  static $hash$imm$fun(val_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
