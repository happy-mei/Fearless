import * as rt from "../rt/main.js";
export class base$$AtomiseStr_0 {
  static $self = new base$$AtomiseStr_0();

  $hash$imm(raw_m$) {
  return base$$AtomiseStr_0.$hash$imm$fun(raw_m$, this);
}

  static $hash$imm$fun(raw_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
