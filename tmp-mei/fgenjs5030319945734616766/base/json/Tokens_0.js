import * as rt from "../rt/main.js";
export class base$$json$$Tokens_0 {
  static $self = new base$$json$$Tokens_0();

  true$imm(line_m$, col_m$) {
  return base$$json$$Tokens_0.true$imm$fun(line_m$, col_m$, this);
}


quoted$imm(line_m$, col_m$, chars_m$) {
  return base$$json$$Tokens_0.quoted$imm$fun(line_m$, col_m$, chars_m$, this);
}


comma$imm(line_m$, col_m$) {
  return base$$json$$Tokens_0.comma$imm$fun(line_m$, col_m$, this);
}


cc$imm(line_m$, col_m$) {
  return base$$json$$Tokens_0.cc$imm$fun(line_m$, col_m$, this);
}


cs$imm(line_m$, col_m$) {
  return base$$json$$Tokens_0.cs$imm$fun(line_m$, col_m$, this);
}


oc$imm(line_m$, col_m$) {
  return base$$json$$Tokens_0.oc$imm$fun(line_m$, col_m$, this);
}


os$imm(line_m$, col_m$) {
  return base$$json$$Tokens_0.os$imm$fun(line_m$, col_m$, this);
}


false$imm(line_m$, col_m$) {
  return base$$json$$Tokens_0.false$imm$fun(line_m$, col_m$, this);
}


null$imm(line_m$, col_m$) {
  return base$$json$$Tokens_0.null$imm$fun(line_m$, col_m$, this);
}


numeric$imm(line_m$, col_m$, num_m$) {
  return base$$json$$Tokens_0.numeric$imm$fun(line_m$, col_m$, num_m$, this);
}


unknownFragment$imm(line_m$, col_m$, bufferContents_m$) {
  return base$$json$$Tokens_0.unknownFragment$imm$fun(line_m$, col_m$, bufferContents_m$, this);
}


colon$imm(line_m$, col_m$) {
  return base$$json$$Tokens_0.colon$imm$fun(line_m$, col_m$, this);
}

  static true$imm$fun(line_m$, col_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static false$imm$fun(line_m$, col_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static null$imm$fun(line_m$, col_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static os$imm$fun(line_m$, col_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static cs$imm$fun(line_m$, col_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static oc$imm$fun(line_m$, col_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static cc$imm$fun(line_m$, col_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static comma$imm$fun(line_m$, col_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static colon$imm$fun(line_m$, col_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static quoted$imm$fun(line_m$, col_m$, chars_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static numeric$imm$fun(line_m$, col_m$, num_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static unknownFragment$imm$fun(line_m$, col_m$, bufferContents_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
