import * as rt from "../rt/main.js";
export class base$$json$$LexNum_0 {
  static $self = new base$$json$$LexNum_0();

  $hash$imm(downstream_m$, ctx_m$, e_m$, parent_m$) {
  return base$$json$$LexNum_0.$hash$imm$fun(downstream_m$, ctx_m$, e_m$, parent_m$, this);
}

  static $hash$imm$fun(downstream_m$, ctx_m$, e_m$, parent_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
