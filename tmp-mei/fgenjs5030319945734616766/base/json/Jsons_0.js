import * as rt from "../rt/main.js";
export class base$$json$$Jsons_0 {
  static $self = new base$$json$$Jsons_0();

  bool$imm(b_m$) {
  return base$$json$$Jsons_0.bool$imm$fun(b_m$, this);
}


number$imm(n_m$) {
  return base$$json$$Jsons_0.number$imm$fun(n_m$, this);
}


array$imm(a_m$) {
  return base$$json$$Jsons_0.array$imm$fun(a_m$, this);
}


object$imm(o_m$) {
  return base$$json$$Jsons_0.object$imm$fun(o_m$, this);
}


null$imm() {
  return base$$json$$Jsons_0.null$imm$fun(this);
}


string$imm(s_m$) {
  return base$$json$$Jsons_0.string$imm$fun(s_m$, this);
}

  static string$imm$fun(s_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static number$imm$fun(n_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static bool$imm$fun(b_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static object$imm$fun(o_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static array$imm$fun(a_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static null$imm$fun($this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
