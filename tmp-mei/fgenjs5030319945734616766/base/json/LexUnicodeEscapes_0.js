import * as rt from "../rt/main.js";
export class base$$json$$LexUnicodeEscapes_0 {
  static $self = new base$$json$$LexUnicodeEscapes_0();

  $hash$imm(ctx_m$, buffer_m$, e_m$) {
  return base$$json$$LexUnicodeEscapes_0.$hash$imm$fun(ctx_m$, buffer_m$, e_m$, this);
}

  static $hash$imm$fun(ctx_m$, buffer_m$, e_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
