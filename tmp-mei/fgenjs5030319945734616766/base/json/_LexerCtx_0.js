import { base$$Stringable_0 } from "../../base/Stringable_0.js";
import * as rt from "../rt/main.js";

export class base$$json$$_LexerCtx_0 extends base$$Stringable_0 {
  static $self = new base$$json$$_LexerCtx_0();

  str$read() {
  return base$$json$$_LexerCtx_0.str$read$fun(this);
}


mode$mut() {
  return base$$json$$_LexerCtx_0.mode$mut$fun(this);
}


mode$mut(next_m$) {
  return base$$json$$_LexerCtx_0.mode$mut$fun(next_m$, this);
}


isEmpty$read() {
  return base$$json$$_LexerCtx_0.isEmpty$read$fun(this);
}


clear$mut() {
  return base$$json$$_LexerCtx_0.clear$mut$fun(this);
}


isNumberStart$read(ch_m$) {
  return base$$json$$_LexerCtx_0.isNumberStart$read$fun(ch_m$, this);
}


isWhitespace$read(ch_m$) {
  return base$$json$$_LexerCtx_0.isWhitespace$read$fun(ch_m$, this);
}


isChar$read(ch_m$) {
  return base$$json$$_LexerCtx_0.isChar$read$fun(ch_m$, this);
}


write$mut(e_m$) {
  return base$$json$$_LexerCtx_0.write$mut$fun(e_m$, this);
}


unpairedSurrogate$mut() {
  return base$$json$$_LexerCtx_0.unpairedSurrogate$mut$fun(this);
}


isDigit$read(ch_m$) {
  return base$$json$$_LexerCtx_0.isDigit$read$fun(ch_m$, this);
}


col$mut() {
  return base$$json$$_LexerCtx_0.col$mut$fun(this);
}


line$mut() {
  return base$$json$$_LexerCtx_0.line$mut$fun(this);
}

  static isEmpty$read$fun($this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static write$mut$fun(e_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static clear$mut$fun($this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static str$read$fun($this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static mode$mut$fun($this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static mode$mut$fun(next_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static isDigit$read$fun(ch_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static isNumberStart$read$fun(ch_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static isWhitespace$read$fun(ch_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static isChar$read$fun(ch_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static unpairedSurrogate$mut$fun($this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static line$mut$fun($this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static col$mut$fun($this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
