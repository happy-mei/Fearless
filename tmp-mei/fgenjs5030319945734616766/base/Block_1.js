import { base$$Sealed_0 } from "../base/Sealed_0.js";
import * as rt from "../rt/main.js";

export class base$$Block_1 extends base$$Sealed_0 {
  static $self = new base$$Block_1();

  if$mut(p_m$) {
  return base$$Block_1.if$mut$fun(p_m$, this);
}


_do$mut(v_m$) {
  return base$$Block_1._do$mut$fun(v_m$, this);
}


loop$mut(body_m$) {
  return base$$Block_1.loop$mut$fun(body_m$, this);
}


openIso$mut(x_m$, cont_m$) {
  return base$$Block_1.openIso$mut$fun(x_m$, cont_m$, this);
}


var$mut(x_m$, cont_m$) {
  return base$$Block_1.var$mut$fun(x_m$, cont_m$, this);
}


done$mut() {
  return base$$Block_1.done$mut$fun(this);
}


isoPod$mut(x_m$, cont_m$) {
  return base$$Block_1.isoPod$mut$fun(x_m$, cont_m$, this);
}


assert$mut(p_m$) {
  return base$$Block_1.assert$mut$fun(p_m$, this);
}


assert$mut(p_m$, failMsg_m$) {
  return base$$Block_1.assert$mut$fun(p_m$, failMsg_m$, this);
}


return$mut(a_m$) {
  return base$$Block_1.return$mut$fun(a_m$, this);
}


let$mut(x_m$, cont_m$) {
  return base$$Block_1.let$mut$fun(x_m$, cont_m$, this);
}


do$mut(r_m$) {
  return base$$Block_1.do$mut$fun(r_m$, this);
}

  static done$mut$fun($this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static return$mut$fun(a_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static do$mut$fun(r_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static _do$mut$fun(v_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static assert$mut$fun(p_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static assert$mut$fun(p_m$, failMsg_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static let$mut$fun(x_m$, cont_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static openIso$mut$fun(x_m$, cont_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static var$mut$fun(x_m$, cont_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static isoPod$mut$fun(x_m$, cont_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static if$mut$fun(p_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static loop$mut$fun(body_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
