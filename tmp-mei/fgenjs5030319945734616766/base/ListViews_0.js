import * as rt from "../rt/main.js";
export class base$$ListViews_0 {
  static $self = new base$$ListViews_0();

  subList$imm(list_m$, from_m$, to_m$) {
  return base$$ListViews_0.subList$imm$fun(list_m$, from_m$, to_m$, this);
}


indexMap$imm(list_m$, mappings_m$) {
  return base$$ListViews_0.indexMap$imm$fun(list_m$, mappings_m$, this);
}


id$imm(list_m$) {
  return base$$ListViews_0.id$imm$fun(list_m$, this);
}

  static id$imm$fun(list_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static subList$imm$fun(list_m$, from_m$, to_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static indexMap$imm$fun(list_m$, mappings_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
