import * as rt from "../rt/main.js";
export class base$$Count_0 {
  static $self = new base$$Count_0();

  nat$imm(n_m$) {
  return base$$Count_0.nat$imm$fun(n_m$, this);
}


int$imm(n_m$) {
  return base$$Count_0.int$imm$fun(n_m$, this);
}


$hash$imm(n_m$) {
  return base$$Count_0.$hash$imm$fun(n_m$, this);
}

  static $hash$imm$fun(n_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static int$imm$fun(n_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static nat$imm$fun(n_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
