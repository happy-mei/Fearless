import * as rt from "../rt/main.js";
export class base$$flows$$_With_0 {
  static $self = new base$$flows$$_With_0();

  $hash$imm(sinkDecorator_m$, upstream_m$, other_m$) {
  return base$$flows$$_With_0.$hash$imm$fun(sinkDecorator_m$, upstream_m$, other_m$, this);
}


impl$imm(downstream_m$, op_m$, other_m$, sinkDecorator_m$) {
  return base$$flows$$_With_0.impl$imm$fun(downstream_m$, op_m$, other_m$, sinkDecorator_m$, this);
}

  static $hash$imm$fun(sinkDecorator_m$, upstream_m$, other_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static impl$imm$fun(downstream_m$, op_m$, other_m$, sinkDecorator_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
