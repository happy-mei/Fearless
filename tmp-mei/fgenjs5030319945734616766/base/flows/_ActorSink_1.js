import { base$$Sealed_0 } from "../../base/Sealed_0.js";
import * as rt from "../rt/main.js";

export class base$$flows$$_ActorSink_1 extends base$$Sealed_0 {
  static $self = new base$$flows$$_ActorSink_1();

  pushError$mut(info_m$) {
  return base$$flows$$_ActorSink_1.pushError$mut$fun(info_m$, this);
}


$hash$mut(x_m$) {
  return base$$flows$$_ActorSink_1.$hash$mut$fun(x_m$, this);
}

  static $hash$mut$fun(x_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static pushError$mut$fun(info_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
