import * as rt from "../rt/main.js";
export class base$$flows$$_Map_0 {
  static $self = new base$$flows$$_Map_0();

  $hash$imm(sinkDecorator_m$, upstream_m$, f_m$) {
  return base$$flows$$_Map_0.$hash$imm$fun(sinkDecorator_m$, upstream_m$, f_m$, this);
}


$hash$imm(sinkDecorator_m$, upstream_m$, ctx_m$, f_m$) {
  return base$$flows$$_Map_0.$hash$imm$fun(sinkDecorator_m$, upstream_m$, ctx_m$, f_m$, this);
}


impl$imm(downstream_m$, ctx_m$, f_m$, sinkDecorator_m$) {
  return base$$flows$$_Map_0.impl$imm$fun(downstream_m$, ctx_m$, f_m$, sinkDecorator_m$, this);
}


impl$imm(downstream_m$, f_m$, sinkDecorator_m$) {
  return base$$flows$$_Map_0.impl$imm$fun(downstream_m$, f_m$, sinkDecorator_m$, this);
}

  static $hash$imm$fun(sinkDecorator_m$, upstream_m$, f_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static impl$imm$fun(downstream_m$, f_m$, sinkDecorator_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static $hash$imm$fun(sinkDecorator_m$, upstream_m$, ctx_m$, f_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static impl$imm$fun(downstream_m$, ctx_m$, f_m$, sinkDecorator_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
