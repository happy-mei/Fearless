import * as rt from "../rt/main.js";
export class base$$flows$$_Actor_0 {
  static $self = new base$$flows$$_Actor_0();

  $hash$imm(sinkDecorator_m$, upstream_m$, state_m$, f_m$) {
  return base$$flows$$_Actor_0.$hash$imm$fun(sinkDecorator_m$, upstream_m$, state_m$, f_m$, this);
}


anyRet$imm(sinkDecorator_m$, upstream_m$, state_m$, f_m$) {
  return base$$flows$$_Actor_0.anyRet$imm$fun(sinkDecorator_m$, upstream_m$, state_m$, f_m$, this);
}

  static $hash$imm$fun(sinkDecorator_m$, upstream_m$, state_m$, f_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static anyRet$imm$fun(sinkDecorator_m$, upstream_m$, state_m$, f_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
