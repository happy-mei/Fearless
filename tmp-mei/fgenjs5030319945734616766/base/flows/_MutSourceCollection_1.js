import * as rt from "../rt/main.js";
export class base$$flows$$_MutSourceCollection_1 {
  static $self = new base$$flows$$_MutSourceCollection_1();

  $hash$imm(source_m$, size_m$) {
  return base$$flows$$_MutSourceCollection_1.$hash$imm$fun(source_m$, size_m$, this);
}


get$imm(cloned_m$, original_m$) {
  return base$$flows$$_MutSourceCollection_1.get$imm$fun(cloned_m$, original_m$, this);
}


wrap$imm(source_m$) {
  return base$$flows$$_MutSourceCollection_1.wrap$imm$fun(source_m$, this);
}


$hash$imm(source_m$) {
  return base$$flows$$_MutSourceCollection_1.$hash$imm$fun(source_m$, this);
}

  static $hash$imm$fun(source_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static $hash$imm$fun(source_m$, size_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static wrap$imm$fun(source_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static get$imm$fun(cloned_m$, original_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
