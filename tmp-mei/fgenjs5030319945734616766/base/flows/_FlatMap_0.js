import * as rt from "../rt/main.js";
export class base$$flows$$_FlatMap_0 {
  static $self = new base$$flows$$_FlatMap_0();

  impl$imm(downstream_m$, f_m$, op_m$, isRunning_m$, sinkDecorator_m$) {
  return base$$flows$$_FlatMap_0.impl$imm$fun(downstream_m$, f_m$, op_m$, isRunning_m$, sinkDecorator_m$, this);
}


$hash$imm(sinkDecorator_m$, upstream_m$, f_m$) {
  return base$$flows$$_FlatMap_0.$hash$imm$fun(sinkDecorator_m$, upstream_m$, f_m$, this);
}


flatten$imm(toFlatten_m$, downstream_m$, op_m$, sinkDecorator_m$) {
  return base$$flows$$_FlatMap_0.flatten$imm$fun(toFlatten_m$, downstream_m$, op_m$, sinkDecorator_m$, this);
}

  static $hash$imm$fun(sinkDecorator_m$, upstream_m$, f_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static impl$imm$fun(downstream_m$, f_m$, op_m$, isRunning_m$, sinkDecorator_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static flatten$imm$fun(toFlatten_m$, downstream_m$, op_m$, sinkDecorator_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
