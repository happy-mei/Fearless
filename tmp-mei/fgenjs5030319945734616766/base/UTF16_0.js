import { base$$Sealed_0 } from "../base/Sealed_0.js";
import * as rt from "../rt/main.js";

export class base$$UTF16_0 extends base$$Sealed_0 {
  static $self = new base$$UTF16_0();

  fromSurrogatePair$imm(high_m$, low_m$) {
  return base$$UTF16_0.fromSurrogatePair$imm$fun(high_m$, low_m$, this);
}


isSurrogate$imm(codePoint_m$) {
  return base$$UTF16_0.isSurrogate$imm$fun(codePoint_m$, this);
}


fromCodePoint$imm(codePoint_m$) {
  return base$$UTF16_0.fromCodePoint$imm$fun(codePoint_m$, this);
}

  static fromCodePoint$imm$fun(codePoint_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static isSurrogate$imm$fun(codePoint_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

    static fromSurrogatePair$imm$fun(high_m$, low_m$, $this) {
  return (function() {
  console.error("Program aborted at:\n" + new Error().stack);
  if (typeof process !== "undefined") process.exit(1);
  else throw new Error("Program aborted");
})()
;
}

}
